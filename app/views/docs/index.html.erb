<h1>All Document Listings</h1>

	<% @docs.each do |doc| %>
		<h4><%= link_to doc.title, doc %></h4>
		<p><%= time_ago_in_words(doc.created_at) %></p>	
		<p><%= truncate(doc.content, length: 50) %></p>
	<% end -%>

	